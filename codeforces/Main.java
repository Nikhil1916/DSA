import java.util.*;
public class Main{
    public static void main(String args[]) {
        // Scanner sc = new Scanner(System.in);
        // int d = sc.nextInt();
        // int totHrs = sc.nextInt();
        // String ans = "NO";
        // ArrayList<Integer> arr = new ArrayList<>();
        // int cal_hours = 0;
        // for(int i=1;i<=d;i++) {
        //     int min_time = sc.nextInt();
        //     int max_time = sc.nextInt();
        //     if(cal_hours+max_time<=totHrs) {
        //         cal_hours+=max_time;
        //         arr.add(max_time);
        //     } else {
        //       System.out.println(totHrs - cal_hours);
        //         if(totHrs - cal_hours >= min_time && totHrs-cal_hours<=max_time) {
        //         arr.add(totHrs - cal_hours);
        //         cal_hours+=max_time-((cal_hours+max_time)-totHrs);
        //         }
        //     }
        // }
        //  if(cal_hours>=totHrs) ans = "YES";
        // System.out.println(ans+" "+cal_hours);
        // if(ans.equals("YES")) {
        //     for(int num:arr) {
        //         System.out.print(num+" ");
        //     }
        // }
        System.out.println('a'-'a');
    }
}